const WebServices = {
    recover_password: {
        get_validate_access_key: '/auth/recover-password',
        change_password: '/auth/change-password-access-key',
    }
};

export default WebServices;

